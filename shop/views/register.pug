body
    include mixins/snackbarMixin
    block content
        include mixins/formsMixin
        +registerUserForm('/user/register')
        +snackbar()
    block scripts
        script.
            //refactor later
            $('form').on('submit', function(event) {
                var data = {}
                genericSubmitForm(data, event, this)
            });
